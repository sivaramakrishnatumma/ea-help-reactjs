import { debounce } from "./debounce";

test("debouce", () => {
  const fn = jest.fn();
  const timer = 300;
  expect(true).toBeTruthy();
  // expect(debounce(fn, timer)).toHaveReturned();
  // expect(debounce(fn, timer)).toEqual()
});
